import 'package:flutter/material.dart';
import '../gallery/gallery_manage_page.dart';

/// 图库管理页面（原通讯录位置）
class PhotoManagerPage extends StatelessWidget {
  const PhotoManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const GalleryManagePage();
  }
}
